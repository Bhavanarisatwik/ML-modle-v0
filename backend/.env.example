# Backend Environment Variables

# MongoDB Connection (REQUIRED)
# Replace <db_password> with your actual password
MONGODB_URI=mongodb+srv://satwikbhavanari_db_user:<db_password>@decoyverseprod.wogbyey.mongodb.net/decoyvers?retryWrites=true&w=majority&tls=true&tlsAllowInvalidCertificates=false

# ML Service URL (REQUIRED)
# For local development: http://localhost:8000
# For production: https://ml-modle-v0-2.onrender.com
ML_API_URL=http://localhost:8000

# Authentication (OPTIONAL)
AUTH_ENABLED=False

# JWT Secret (REQUIRED if AUTH_ENABLED=True)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-key-here

# Server Port (OPTIONAL)
PORT=8001

# -----------------------------------------------------------
# Notification Integrations (all OPTIONAL)
# Leave blank to disable that channel
# -----------------------------------------------------------

# Slack Integration
# Webhook URL from your Slack app (Incoming Webhooks)
SLACK_WEBHOOK_URL=

# Email Integration (SMTP)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
ALERT_EMAIL_TO=security-team@yourdomain.com #this should be the user mail so this should be dynamic

# WhatsApp Integration (via Twilio)
# Sign up at https://www.twilio.com/ — use Sandbox number for dev
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_WHATSAPP_NUMBER=your_twilio_number
ALERT_WHATSAPP_TO=+1234567890 #this should be the user number so this should be dynamic

# -----------------------------------------------------------
# Threat Intelligence (all OPTIONAL)
# -----------------------------------------------------------

# AbuseIPDB — free 1 000 checks/day, enriches network alerts with IP reputation
# Register at https://www.abuseipdb.com/register
ABUSEIPDB_API_KEY=your_abuseipdb_api_key
